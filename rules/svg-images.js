// =============================================================================================================================
// RULES - SVG IMAGES
// =============================================================================================================================
module.exports = (svgoOptions = {}) => ({
  test: /\.svg$/,
  use: [
    {
      loader: "react-svg-loader",
      options: {
        jsx: true,
        svgo: svgoOptions,
      },
    },
  ],
});
